import {
  handleFileImport,
  handleListDelete,
  handleListExport,
  handleListRestore,
} from ".";

const ListTransferAPI = {
  handleListExport,
  handleFileImport,
  handleListDelete,
  handleListRestore,
};
export { ListTransferAPI };
